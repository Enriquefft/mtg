.venv/
.env
.direnv/
__pycache__/
*.pyc
*.egg-info/
**/node_modules/
result
result-*

# .NET build artifacts (tools/inject/)
tools/inject/build/
**/bin/
**/obj/

# Claude Code agent scratch space — settings.json is checked in, the rest is ephemeral
.claude/worktrees/
.claude/local/
.claude/scheduled_tasks.lock

# Decks: existing tracked lists stay tracked; new entries (corpus dumps from
# fetch-meta, ad-hoc drafts) are ephemeral. Use `git add -f` for new curated decks.
decks/
