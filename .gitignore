.venv/
.env
.direnv/
__pycache__/
*.pyc
*.egg-info/
**/node_modules/
result
result-*

# .NET build artifacts (tools/inject/)
tools/inject/build/
**/bin/
**/obj/

# Claude Code agent scratch space — settings.json is checked in, the rest is ephemeral
.claude/worktrees/
.claude/local/
.claude/scheduled_tasks.lock

# `decks/` holds tracked human-curated drafts (e.g. decks/<name>/v*.txt).
# Machine-managed meta corpus from `fetch-meta` lives under `data/corpus/`
# (gitignored separately via data/.gitignore).
